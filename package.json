{
  "name": "mosu",
  "version": "0.7.0",
  "description": "tracks the progress done of osu! maps",
  "keywords": [
    "tauri",
    "osu!",
    "map"
  ],
  "license": "MIT",
  "author": "fax1015",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tauri dev",
    "dev": "npm run sync-version && tauri dev",
    "dev:fresh": "npm run sync-version && node scripts/dev-fresh.js",
    "build": "npm run sync-version && npm run clean && tauri build",
    "package": "tauri build",
    "make": "tauri build",
    "dev:watch": "tauri dev",
    "clean": "node scripts/clean-target.js",
    "sync-version": "node scripts/update-version.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
